{
    "devCommand": "python -m uvicorn index:app --reload",
    "builds": [
        {
          "src": "app.py",
          "use": "@vercel/python",
          "config": {
            "maxDuration": 60,
            "pythonVersion": "3.9"
          }
        }
      ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "index.py"
      }
    ]
  }