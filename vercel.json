{
    "devCommand": "uvicorn main:app --host 0.0.0.0 --port 3000",
    "builds": [
        {
          "src": "app.py",
          "use": "@vercel/python",
          "config": {
            "maxDuration": 60,
            "pythonVersion": "3.9"
          }
        }
      ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "api/index.py"
      }
    ]
  }